[pytest]
markers =
    docs: documentation and docs-workflow tests
    integration: cross-component or workflow integration tests
    regression: regression coverage for previously observed failure modes
    slow: long-running stress tests
